apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cool.fullname" . }}-test-connection"
  labels:
    {{- include "cool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
